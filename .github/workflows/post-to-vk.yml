name: Post to VK

on:
  workflow_dispatch:

jobs:
  post:
    runs-on: ubuntu-latest

    steps:
      - name: Test post to VK
        env:
          VK_TOKEN: ${{ secrets.VK_COMMUNITY_TOKEN }}
          VK_GROUP_ID: ${{ secrets.VK_GROUP_ID }}
        run: |
          RESPONSE=$(curl -s -X POST "https://api.vk.com/method/wall.post" \
            -d "owner_id=-${VK_GROUP_ID}" \
            -d "from_group=1" \
            --data-urlencode "message=Тестовый пост из GitHub Actions. Проверяем публикацию без фото." \
            -d "access_token=${VK_TOKEN}" \
            -d "v=5.199")

          echo "$RESPONSE"

          if echo "$RESPONSE" | grep -q '"error"'; then
            echo "VK API returned error"
            exit 1
          fi
